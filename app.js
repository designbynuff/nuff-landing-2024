window.addEventListener('load', () => {

    // When a cursor hovers over a span, show the corresponding image

    // It could be fun to reveal a secret n+1th thing if you hover over all the other things. Like a "well done, you figured it out, here's a video or something"

});